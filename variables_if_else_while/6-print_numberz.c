#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - prints numbers
 * Return: value 0.
 */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	putchar(n);
	putchar ('\n');

	return (0);
}
