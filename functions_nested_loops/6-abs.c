#include "main.h"

/**
 * _abs - prints absolute value of integer
 * @a: the input character to compare
 *
 * Return: returns the input character
 */

int _abs(int a)
{
	if (a > 0)
	{
		return (a);
	}
	return (-a);
}
